---
name: content-pipeline
description: >
  全自动内容创作流水线 — 热点监控→选题筛选→深度撰写→格式排版。
  人工介入 <15 分钟/篇，初稿可用度 >70%。严禁全自动发布。
  用于“帮我写一篇文章”、“今天有什么热点”、“内容创作”、“选题”等场景。
  不用于短消息回复、代码生成或数据分析。
author: Survival Team
version: 1.1.0
created: 2026-01-20
updated: 2026-02-15
agents: [general]
---

# 🔥 Content Pipeline — 全自动内容创作流水线

> **核心原则：** Human-in-the-loop。系统负责效率，你负责判断。严禁全自动发布。

## ⚡ 一句话说明

这个 Skill 把"热点监控 → 选题筛选 → 初稿撰写 → 格式排版"这条链路自动化了。你唯一要做的就是：**选题时回复一个数字，审稿时花 10 分钟改改**。

## 📁 文件结构

```
content-pipeline/
├── SKILL.md           # 你正在看的这个文件 — 总指南
├── SOUL.md            # 人设与边界 — Agent 写作的"宪法"
├── STYLE.md           # 风格规范 — 3000 字写作样本库
├── scripts/
│   ├── pipeline.py    # 🎯 主入口 — 一键跑完整条流水线
│   ├── scout.py       # 📡 阶段1 — 热点侦察 + 选题筛选
│   ├── draft.py       # ✍️ 阶段2 — 深度撰写
│   └── memory.py      # 🧠 记忆管理 — 防撞车、防矛盾
├── config/
│   └── pipeline.yaml  # 流水线配置（关键词、频率、偏好）
└── memory/
    └── .gitkeep       # 记忆日志目录（YYYY-MM-DD.json）
```

## 🚀 使用方法

### 方式一：全自动流水线（推荐）

```bash
# 一键跑完"侦察 → 选题 → 写作 → 飞书"
python /app/skills/content-pipeline/scripts/pipeline.py run

# 仅侦察选题（不写作）
python /app/skills/content-pipeline/scripts/pipeline.py scout

# 指定选题直接写作
python /app/skills/content-pipeline/scripts/pipeline.py draft \
  --topic "OpenAI 发布 GPT-5，参数量突破 10 万亿" \
  --angle "从创业者角度看 GPT-5 对 SaaS 行业的冲击"
```

### 方式二：分步调用

#### 阶段 1：热点侦察

```bash
# 搜索最近 24h 热点，输出 3-5 个选题
python /app/skills/content-pipeline/scripts/scout.py \
  --keywords "AI Agent,SaaS,出海" \
  --count 5 \
  --output /tmp/nanobot/topics.md
```

输出格式：

```
[9.2] OpenAI 发布 GPT-5 | 参数量 10T，推理成本降 80%，API 定价策略引发争议
[8.8] Cursor 融资 1 亿美金 | AI 编程赛道白热化，对独立开发者意味着什么
[8.5] 字节跳动 Seedance 3.0 | 视频生成从"玩具"到"生产力"的临界点
```

你回复数字即可（如 "1"），流程自动进入下一阶段。

#### 阶段 2：深度撰写

```bash
# 根据选题生成初稿
python /app/skills/content-pipeline/scripts/draft.py \
  --topic "OpenAI 发布 GPT-5" \
  --angle "创业者视角" \
  --output /tmp/nanobot/draft.md
```

#### 阶段 3：交付飞书

```bash
# 调用已有 feishu skill 上传
python /app/skills/feishu/scripts/feishu.py create_doc \
  --title "[草稿] GPT-5 对 SaaS 行业的冲击" \
  --file /tmp/nanobot/draft.md
```

#### 阶段 4：人工审核 + 发布

你打开飞书文档 → 修改语气 → 确认数据 → 补充个人经历 → **手动发布**。

## 🧠 双模型协作

本 Skill 严格遵守 nanobot 的双模型架构：

| 步骤         | 由谁执行               | 说明                       |
| ------------ | ---------------------- | -------------------------- |
| 热点搜索     | V3 (你)                | `web_search` + `web_fetch` |
| 搜索结果筛选 | V3 (你)                | 调用 scout.py 评分排序     |
| 初稿生成     | **R1** (llm-processor) | 长文创作必须用 R1          |
| 格式整理     | V3 (你)                | 调用 draft.py 后处理       |
| 飞书创建     | V3 (你)                | 调用 feishu skill          |
| 记忆更新     | V3 (你)                | 调用 memory.py 记录        |

### 典型调用链

```bash
# 1. 搜索热点
web_search("AI Agent framework github trending 最新争议")
web_fetch("https://...")

# 2. 保存素材
write_file /tmp/nanobot/raw_material.md "搜索到的内容..."

# 3. R1 生成初稿 (注意：读取 SOUL.md 和 STYLE.md 作为 system prompt)
python /app/skills/llm-processor/scripts/llm_process.py custom \
  --prompt "$(cat /app/skills/content-pipeline/SOUL.md /app/skills/content-pipeline/STYLE.md) \n\n请根据以下素材撰写一篇深度文章" \
  --file /tmp/nanobot/raw_material.md \
  --output /tmp/nanobot/draft_raw.md

# 4. 后处理（插入金句锚点、格式检查）
python /app/skills/content-pipeline/scripts/draft.py \
  --raw /tmp/nanobot/draft_raw.md \
  --output /tmp/nanobot/draft_final.md

# 5. 上传飞书
python /app/skills/feishu/scripts/feishu.py create_doc \
  --title "[草稿] 文章标题" \
  --file /tmp/nanobot/draft_final.md

# 6. 记录记忆
python /app/skills/content-pipeline/scripts/memory.py log \
  --topic "文章标题" \
  --stance "看多 AI Agent" \
  --data-cited "GPT-5 参数 10T, 推理成本降 80%"

# 7. 清理
exec rm -rf /tmp/nanobot/*
```

## ⚠️ 避坑指南

### 前两周磨合期

- **不要指望直接发布。** 把每一次修改都记录下来，反向更新到 `STYLE.md` 中。
- Agent 是"喂"出来的，不是一次配置好的。

### 图片是大坑

- 初期 Agent 只负责在文档中写 `[📷 这里放一张关于 XXX 的图]`。
- **由人工补图**，别指望 AI 找图。

### 事实核查

- 涉及具体的**数字、人名、代码版本号**，**必须人工复核**。
- Agent 在这方面依然存在幻觉风险，这是铁律。

## 🔗 依赖的 Skill

| Skill           | 用途                 |
| --------------- | -------------------- |
| `llm-processor` | 调用 R1 生成长文内容 |
| `feishu`        | 创建飞书文档         |
| `web-scraper`   | 抓取网页内容（可选） |

## 📊 预期指标

| 指标         | 目标                   |
| ------------ | ---------------------- |
| 人工介入时间 | < 15 分钟/篇           |
| 初稿可用度   | > 70%                  |
| 选题环节     | 0 分钟（回复数字即可） |
| 写作环节     | 3-5 分钟（R1 生成）    |
| 排版环节     | 0 分钟（自动上传飞书） |
| 审核环节     | 10-15 分钟（人工精修） |

## 演化日志

| 日期       | 版本  | 变更                     | 触发原因       |
| ---------- | ----- | ------------------------ | -------------- |
| 2026-02-15 | 1.1.0 | 对齐 SKILL-STANDARD 规范 | 标准化统一优化 |
| 2026-01-20 | 1.0.0 | 初始版本                 | —              |
